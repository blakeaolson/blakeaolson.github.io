---
layout: post
title: "Paper: Simple and Effective Masked Diffusion Language Models"
date: 2026-04-10
tags: [LLM, Diffusion]
---
Recently, diffusion language models have grown popular by offering theoretical inference speed-ups and an alternative to autoregressive models (ARMs).

Intuitively, humans do not think word by word. We have different thoughts that collide and take shape after multiple iterations. This is why diffusion makes sense: The model does not have to condition its output based on previous words. It can provide an output based on the entire sequence and iteratively refine it.

This paper removes reliance on ARMs. While built on a transformer architecture, it relies on bi-directional attention with a masked language model objective (similar to BERT). The goal is not to generate tokens one by one, but to iteratively demask words from a fixed-length sequence.

##### Implementation